[project]
name = "apiregen"
version = "0.4.0"
description = "Interactive API reverse engineering toolkit for MCP-compatible AI assistants"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.26.0",
]

[project.scripts]
apiregen = "apiregen.cli:cli"
apiregen-mcp = "apiregen.mcp_server:main"

[project.optional-dependencies]
cli = ["click", "rich"]
browser = ["camoufox[geoip]"]
mitmproxy = ["mitmproxy"]
all = ["apiregen[cli,browser,mitmproxy]"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest",
    "apiregen[cli]",
    "build>=1.4.4",
    "hatchling>=1.29.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/apiregen"]

[tool.pytest.ini_options]
testpaths = ["tests"]
